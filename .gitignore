__pycache__/
*.pyc
.pytest_cache/
.aegis-cache/
*.swp
.DS_Store

# Project-local Claude Code state (per-user hooks, ephemeral worktrees)
.claude/

# Third-party agent session state that lands in the repo root
.omo/

# OpenCode scaffolding. The adapter imports only node: builtins, so nothing
# under node_modules is needed to run it or its tests.
.opencode/node_modules/
.opencode/package-lock.json
.opencode/bun.lock
